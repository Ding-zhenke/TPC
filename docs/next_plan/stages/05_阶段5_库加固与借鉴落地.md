# 阶段 5：cst_solver 加固与借鉴落地

> 状态：🔄 **进行中（不需要 CST 的部分已全部做完；需 CST 验证的 3 项挂起）**
> 对应旧编号：**新增阶段** —— 内容来自 `cst-runtime-cli` 采纳计划批次 1/2/3、
> 旧 06 §九「TPC 库修改清单」+ §14.5「未排期的库层任务」、旧 04 §3 的 28 条库层任务、
> 以及 `cst_solver_TODO_LIST.md` 的 VBA 覆盖缺口
> 上游：阶段 4（至少几何验收通过）· 下游：阶段 6
>
> **本轮做完的**（2026-09-15，全部**不需要 CST**，已实测）：5.1 / 5.2 / 5.3 / 5.4 / 5.5 / 5.6 /
> 5.7.1 / 5.7.2 / 5.8 的端口增强部分。详见 §3「本轮实测结果」。
> **挂起的**：5.1.3（真 CST 跑模板）、5.2.2 的实测部分、5.8 的低频求解器族 —— 都因为
> 用户要求**暂不启动新的 CST 计算**。已登记在 §5。

---

## 1. 阶段目标

> 把「**读文档才知道**」变成「**写错就提示**」。

当前 `cst_solver` 的覆盖度其实不低（23 Mixin / 222 公开方法），但**约束全是文档约定**：
绕向、布尔语义、面编号、参数重建……全都写在 `ARCHITECTURE.md` §6 里，靠人记住。
阶段 4 暴露的 8 处缺陷里有 6 处属于这一类（"从未被真实调用过"）。

---

## 2. 任务清单

### 5.1 运行时守卫层（采纳计划批次 1）

**先说清一个前提**：采纳计划 §0 标记的「批次 0 可行性验证」**已经完成**：

- 实测结论：`StoreParameter` + `full_history_rebuild()` 这条路的**有效性尚未单独验证**，
  但 `app.update()`（→ `full_history_rebuild()`）已在真实会话中反复成功调用；
  真正踩到的坑是**另一族**（见 §2.4）。
- 因此守卫设计不再悬在假设上，可以按「拦截 + 提示 `update()`」的轻量方案走。

| 任务 | 内容 |
|---|---|
| 5.1.1 | 新增 `cst_solver/_guards.py`（**纯新增，零风险**） |
| 5.1.2 | `topo_modeler/modeler.py` 接线（方案 A：零侵入 `cst_solver`） |
| 5.1.3 | 回归：跑两个模板确认**不误报**（它们的 `para` 在建工程前，应判 clean） |
| 5.1.4 | **构造反例**：`modeler.set_parameter('g', 25).run()` → 必须报警 |
| 5.1.5 | 通过后扩到方案 B：`cst_solver` 三处追加（`para` / `update` / `run`） |
| 5.1.6 | `mode='off'` 时行为与改动前**逐字节一致**（回归基线） |
| 5.1.7 | 同步 `setup.pyi` + 重跑生成器 |

**守卫分级**（借它的设计，但去掉不适用的部分）：

| 编号 | 陷阱 | 处理 |
|---|---|---|
| **T2** | 改参后未重建模型就直接仿真 | 🔴 **硬拦截** + `next_action` |
| T3 | 远场导出后 save 损坏工程 | 强制 `save=False`（需先普查） |
| T8 | `Abs(E)` 当增益证据 | 白名单 `{Realized Gain, Gain, Directivity}` |
| T10 | `project_path` 非法 | 校验后缀 `.cst` / `.prj` |
| T13 | 只改参数表不重建 | 🟡 **警告**（与 T2 同根因，不同处置点） |
| T15 | save 与 close 顺序 | 先 `save()` 再 `close()` |
| **T7'** | `log_flag=0` 只返文本不下发，用户误当已执行 | **本项目特有**，对方没有 |

> **不能照搬的部分**：它的 `session_type` 状态机、disk marker、DE 进程管理
> —— 那些是为 CLI 的进程生命周期设计的，本库是**长驻 `setup` 对象**。
> **可搬的**：陷阱目录、严重度分级、错误三件套（code / message / next_action）。

### 5.2 补守卫与结构化验收 API（采纳计划批次 2）

| 任务 | 内容 |
|---|---|
| 5.2.1 | `postprocessing/farfield.py` 的 `farfield_plot_polar(mode=...)` 加白名单校验 |
| 5.2.2 | **T3 普查**：确认远场导出后是否会触发 `save()` |
| 5.2.3 | **T7' 普查**：`log_flag=0` 被误当已执行的风险 |
| 5.2.4 | `project.py::open_project()` 加后缀校验 |
| 5.2.5 | `project.py::close()` 保证 save-before-close 顺序 |
| 5.2.6 | 新增 `cst_solver.validate_model()` —— 结构化验收 API |
| 5.2.7 | 新增 `cst_solver/tests/test_guards.py`（**可改写自对方的 `test_gateway.py`**） |

**`validate_model()` 补的是硬约定 §3 的缺口**：
「验收必须读 `get_messages()` 并跑 `Rebuild()`」目前只是**口头约定，无 API 支撑**。

```python
def validate_model(self):
    """
    验收：读 CST 消息 + 跑 Rebuild，返回结构化结果。

    :return: dict, {"status": "success"|"error",
                    "messages": [...], "rebuild_ok": bool}
    """
```

### 5.3 外部参考资料落地（采纳计划批次 3，**收益最快、可与其它并行**）

| 任务 | 内容 |
|---|---|
| 5.3.1 | 从已 clone 的 `cst-runtime-cli` 取 4 个 reference 文件 |
| 5.3.2 | 存放 `docs/references/` |
| 5.3.3 | 写 `docs/references/THIRD_PARTY_NOTICES.md`（MIT 许可原文 + 来源链接）**必须** |
| 5.3.4 | 在 `skills/developer/cst-solver-dev.md` 增一行指向新参考（替代"翻 CST Help"的说明） |
| 5.3.5 | 抽查：用 `vba-official-reference.md` 核对 `cst_solver` 里 3–5 个 VBA 命令拼写 |

**待落地的 4 个文件**（本机已在仓库外 `D:\成电博士生涯\自动建模算法尝试\cst-runtime-cli\devkit\references\`）：

| 文件 | 大小 | 用途 |
|---|---|---|
| `vba-official-reference.md` | 71.4 KB | VBA API 参考 —— **本轮修求解器属性就是靠它** |
| `cst-official-api-reference.md` | 56.3 KB | CST Python API 参考（`cst.interface` / `cst.results`） |
| `tool-development-guide.md` | 15.1 KB | 工具开发流程（含新增 VBA 对象的方法论） |
| `Test_kit_README.md` | 4.7 KB | 测试体系设计（分层、合约测试、缓存机制） |

> ⚠️ **已知与文档不符**：采纳计划 §1.2 提到的 `devkit/tools/vba_defs/`（10 个 TOML 参考实现）
> 在该仓库 HEAD 上**不存在**（`git ls-files` 零命中）—— 是上游文档领先于代码，不是 clone 不全。

### 5.4 清掉三条文档旧账

| 任务 | 内容 |
|---|---|
| 5.4.1 | `skills/developer/cst-solver-dev.md` 的「待修清单」5 行**已全部过期**（对应缺陷早修掉），按 WORKFLOW §6 矩阵应删除 |
| 5.4.2 | 文档生成器在 **GBK 控制台会直接崩**：`gen_cst_solver_docs.py:1041` 用 `print("\U0001f50d ...")` → `UnicodeEncodeError`。需换 ASCII 或给 stdout 加容错 |
| 5.4.3 | `cst-solver-dev.md` / 采纳计划里的 `python scripts/gen_docs.py` **命令名是错的**，实际是 `gen_cst_solver_docs.py`（前者已修，需全仓扫一遍残留） |

### 5.5 未排期的库层任务（06 §14.5 / 04 §3 的 28 条）

| 新方法 | 处理建议 |
|---|---|
| `get_face_id_by_normal()` | ✅ 已由 `get_face_id_from_point()` 覆盖（阶段 4 §2.4 已验证），**旧名作废** |
| `para_batch()` | ❌ **不做** —— 已由 `paras()` / `set_parameters()` 覆盖，不必新增第三个同义 API |
| `read_all_s_parameters()` | 排入本阶段（结果读取，阶段 7 会用到） |
| `export_s_parameters_csv()` | 排入本阶段 |
| `export_farfield_csv()` | 排入本阶段 |
| `auto_port_at_solid()` | ✅ 已由 `create_waveguide_port_free()` 覆盖 |

### 5.6 CST 表达式生成规范化（06 §14.6）

`TopoPath.lattice_to_cst_expr()` 目前只做极简特判，会产出 `'0*a'` 之类冗余表达式。需定义：
去零项、合并同类项、避免浮点尾数、平方根一律写 `sqr(3)`（CST 语法，**不是** Python 的 `sqrt(3)`）。

### 5.7 资源生命周期与并发（06 §14.7）

| 任务 | 内容 |
|---|---|
| 5.7.1 | `topo_templates/*` 目前**没有 `close()`**；`setup` 支持 `with` 与 `close()`，模板应补上 |
| 5.7.2 | 明确 CST 的 **COM 单实例 / 许可**约束下的并发模型（阶段 7 的 `BatchModeler` 依赖它） |

### 5.8 `cst_solver_TODO_LIST.md` 的 VBA 覆盖缺口

**优先级 1（常用）**：

| VBA 对象 | 说明 | 本轮状态 |
|---|---|---|
| `WaveguidePort` 增强 | 端口模式配置、极化角、参考面 —— 现有 `add_port` 太简单 | ✅ **已做**：`add_port()` / `create_waveguide_port_free()` 把写死的 `NumberOfModes` / `AdjustPolarization` / `PolarizationAngle` / `ReferencePlaneDistance` 开放为**关键字参数**（默认值逐字节不变，有回归测试） |
| `EStaticSolver` / `MStaticSolver` / `StationaryCurrentSolver` / `LFSolver` | 低频 / 静电 / 静磁 / 稳电流 | ⏸ **本轮不做**，理由见 §5 |

优先级 2–4（进阶求解器、更多导入导出格式、高级场评估）见 `cst_solver_TODO_LIST.md`，
**按需再排**，不阻塞后续阶段。

---

## 3. 本轮实测结果（2026-09-15，全部不需要 CST）

### 5.1 运行时守卫层 ✅

新增 `cst_solver/_guards.py`（**纯标准库，不 import CST**）：

- 错误三件套 `GuardFinding(code, severity, message, next_action, plan_id)` + `CstGuardError`；
- 三档模式 `off` / `warn`（默认）/ `strict`；可用 `set_guard_mode()` 或 `config.py` 的
  `CST_GUARD_MODE` 切换；
- 状态**按宿主对象隔离**（`get_guard_state(app)`），不用全局单例，不同 `setup` 互不干扰；
- 接线：**方案 A**（`topo_modeler` 的每个 builder 后 `mark_geometry_built()`）+
  **方案 B**（`cst_solver` 三处：参数 / 求解 / 项目生命周期）。

| 编号 | 陷阱 | 触发点 | 处置 |
|---|---|---|---|
| T2 | 改**已被几何引用**的参数后直接 `run()` | `run()` | strict 抛异常 / warn 警告 |
| T7'/T13 | `para(..., log_flag=0)` 写参数表但不重建历史 | `para()`/`paras()` | 警告 |
| T3 | 导出远场/2D-3D 后再 `save(include_results=True)` | `save()` | 警告，建议 `include_results=False` |
| T8 | `Abs(E)`（`'efield'`）当增益证据 | 远场绘图 | 未知模式拦截 + `require_gain=True` 断言 |
| T10 | 工程路径后缀非法 | `open_project()` | strict 抛错 |
| T15 | `close()` 之后再 `save()` | `save()`/`close()` | 先 save 后 close；未保存就关给警告 |

**关键回归保证**：`mode='off'` 与改动前**逐字节一致** —— 不报警、不拦截、
**不额外调用任何 CST 接口**（连几何探针都不问）。有专门用例守着。

### 5.2 结构化验收 ✅

- 新增 `cst_solver/validation.py` 的 `ValidationMixin`：
  `get_messages()` + `validate_model(rebuild=True)` → `{status, messages, rebuild_ok, before, guard}`。
  把 WORKFLOW §3 那条「验收必须读 `get_messages()` 并跑 `Rebuild()`」的口头约定变成 API；
- `project.py` 加后缀校验（T10）与 save-before-close（T15）；
- 远场白名单落地为 `check_farfield_mode(mode, require_gain=...)`，并给
  `farfield_plot_polar` / `farfield_plot_cartesian` / `set_farfield_plot` 加了 `require_gain` 形参。

### 5.3 外部参考资料落地 ✅

`docs/references/` 下 4 份文件**逐字节复制**并校验 SHA-256（源=目标），
`THIRD_PARTY_NOTICES.md` 收录上游 **MIT 许可原文**（字符级校验一致）+ 来源 URL
（[github.com/bbl21/cst-runtime-cli](https://github.com/bbl21/cst-runtime-cli)，HEAD `7eac8219`）。
另做了 **5 项 VBA 拼写抽查**（见 `docs/references/README.md` 末节），结果：
4 项与参考一致，**1 项存疑**（`.Coordinates "Picks"` vs 参考写 `"Picked"`），
1 项**在参考中查不到**（`Solid.Imprint`）—— 两条都记进 §4。

### 5.4 三条文档旧账 ✅

| 项 | 结果 |
|---|---|
| 5.4.1 | `cst-solver-dev.md` 待修清单已清空并附对照表（前轮完成） |
| 5.4.2 | 去 emoji 已完成；**本轮又扫出 4 个脚本的同类崩溃**（见 §4 第 4 条），一并修掉，GBK 下实测 exit 0 |
| 5.4.3 | 全仓扫残留：`cst_solver_TODO_LIST.md` 3 处、采纳计划 1 处已改为 `gen_cst_solver_docs.py`；`archive/` 与 WORKFLOW 的 grep 模式保留（前者是历史归档，后者正是用来抓这类残留的） |

### 5.5 结果读取与导出 ✅（`export_farfield_csv` 未验证）

`cst_solver/_result_core.py` 新增 4 个方法：`list_s_parameters()` /
`read_all_s_parameters()` / `export_s_parameters_csv()` / `export_farfield_csv()`，
全部**离线**（不需要 DE，不占求解器许可）。前三个的底层读取路径与
阶段 4 T6 实测过的 `read_s_parameter()` 同源；`export_farfield_csv` **没有真实工程可试**，
已登记为未验证风险。

### 5.6 `lattice_to_cst_expr` 规范化 ✅

去零项（`(0,0)` → `('0','0')`，不再是 `'0*a'`）、合并同类项（`(14,4)` → `'11*a'`，
不再是 `'4*a+14*a/2'`）、省略系数 1、整值浮点按整数处理、平方根统一 `sqr(3)`。
**py 刻意保持 `r*a/2*sqr(3)`**，与符号式及参考工程写法一致，便于逐条对照。
符号路径的写法**不受影响**（有专门用例钉住）。单测 16 → **18 项**，全绿。

### 5.7 资源生命周期 ✅

- `TopoModeler` 与两个模板都补了 `close()`、`__enter__` / `__exit__`；
- 并发模型写进 `skills/developer/cst-solver-dev.md` 的「并发与资源生命周期」一节，
  按**证据强度分三档**（实测过 / 只观察过一次 / 未验证），不把推测当结论。

### 5.8 WaveguidePort 增强 ✅（低频求解器族不做）

`add_port()` / `create_waveguide_port_free()` 开放 4 个关键字参数；
**逐字节回归**：7 个用例（Picks×4 + Free×3）与 `git HEAD` 版本完全相同，
基线固化成 `cst_solver/tests/data/expected_port_vba.json`。

### 测试与文档同步

| 项 | 结果 |
|---|---|
| 新增单测 | `cst_solver/tests/test_guards.py`（43 条）+ `cst_solver/tests/test_ports_vba.py`（14 条），`pyproject.toml` 的 `testpaths` 已加 `cst_solver/tests` |
| 全量测试 | `python -m pytest mesh_grid -q` → 18 passed；`python -m pytest cst_solver/tests -q` → 57 passed |
| 铁律 §2 | `setup.pyi` 已同步（`ValidationMixin` / `require_gain` / 端口关键字参数）；`gen_cst_solver_docs.py` 已把 `ValidationMixin` 加进 `CATEGORIES` 与扫描列表，重跑后 **225 个方法 / 24 个类别**（原来 223 / 23，新方法此前根本不出现在 API 页里） |
| 文档计数 | `docs/packages/cst_solver.md`、`docs/ARCHITECTURE.md`、`README.md`、`.github/copilot-instructions.md`、`skills/developer/conventions.md` 的「23 个 Mixin / 221 个方法」全部更正为 AST 实测值 |

---

## 4. 本轮遇到的问题与更正（留痕，避免重踩）

| # | 问题 | 处理 |
|---|---|---|
| 1 | 🔴 **原计划的守卫判据会误报** —— 计划写的是「几何已存在就判脏」，但 `topo_modeler/builders/feed.py:145` 会在几何已建好之后才 `app.para('tx1', 0.2)`，而 `tx1` 只被**紧接着**创建的那个矩形引用，不会让任何已有几何变旧 | 判据改为「**只把改一个已存在的参数判成脏**」：首次写入的参数名不可能被已有几何引用。为此加了一个 `GetParameter(name)` 探针，兜住「打开已有工程后第一次改一个早就存在的参数」这个场景。代价（读不出来时按新参数处理 → 漏报一次）已写进 `docs/packages/cst_solver.md` §7.0 |
| 2 | **`log_flag` 在两类方法里含义不同，文档却当成一回事** —— `docs/packages/cst_solver.md` 原来写「`log_flag=0` 时方法只返回 VBA 文本、不下发」，并把 `para`/`paras` 列在同一批里。实际上几何类方法（默认 `1`）的 `0` = 只返回文本；`para`/`paras`（默认 `0`）的 `0` = **写入参数表但不重建历史** —— 参数是**真的存进去了**，所以从返回值上完全看不出异常 | 在 `docs/packages/cst_solver.md` §7.1 加了对照表并明确「不要互相类推」；这正是 T7'/T13 的根源 |
| 3 | **远场 T8 的白名单不能照计划那样无条件执行** —— 计划写「`farfield_plot_polar(mode=...)` 加白名单校验」，但 `'efield'` 是该函数的**合法**用途（画场分布），无条件拦会误伤 | 改成两层：**未知模式**一律拦（挡拼写错误），**是否当增益证据**由调用方用 `require_gain=True` 显式声明 |
| 4 | **同一族的 GBK 控制台崩溃远不止计划里提到的那一处** —— 计划只写了 `gen_cst_solver_docs.py:1041` | 用 **AST** 找出所有 `print(...)` 调用的**行范围**（含多行 f-string 的续行 —— 只按「行里有 print 字样」扫会漏掉续行），全仓扫描后**清零**：`bench_grid_draw.py`、`check_md_links.py`、`compare_model_history.py`、`gen_mesh_docs.py`、`cst_solver/__init__.py`（**在导入路径上**：缺 `config.py` 时 `import cst_solver` 会直接崩）、`cst_solver/material/materials.py`、`mesh_grid/tri_grid/tests/test_topo_path.py`、`topo_modeler/lens_build.py`。`check_md_links.py` 实测：GBK 下原来 exit 1，现在 exit 0。**只改 print 里的字符串字面量，不动任何逻辑** |
| 5 | **API 文档生成器不认新 Mixin** —— `ValidationMixin` 加进 `setup` 之后，重跑生成器仍是 223 个方法，`validate_model` **根本不出现在 API 页里**（生成器的 `CATEGORIES` 与 `modules` 是硬编码列表） | 把 `ValidationMixin` 加进两处列表，重跑 → 225 个方法 / 24 个类别。**这一步以前没人做过**（新增 Mixin 会静默丢文档），已写进 `cst-solver-dev.md` 的步骤 5 |
| 6 | **文档里的方法计数早已与代码不符** —— `docs/packages/cst_solver.md` 写「23 Mixin 共 211 + Result 9 + setup.open 1 = 221」，但 AST 实测 HEAD 就是 `23 Mixin / 221 个方法`（211 那个数是错的，总数 221 是蒙对的）；`picks.py` 标 8 实为 13、`solver.py` 标 23 实为 27、`ports.py` 标 9 实为 10 | 用 `git show HEAD:...` + AST 重算，全部更正为实测值；`conventions.md` 里「23 个 Mixin / 221」改为「24 / 223」 |
| 7 | **`.Coordinates "Picks"` 的拼写存疑** —— vendored 的 `vba-official-reference.md`（L1189）写 `"Free"` / `"Picked"`，本库与参考工程都写 `"Picks"`。同一份资料在别处（L562/574/585）又用 `"Picks"`，属**资料内部不一致** | **不动**（用户此前也明确「是 picks 还是 picked 暂时不管」），记为待真 CST 核实项 |
| 8 | **`Solid.Imprint` 在参考里查不到** —— `booleans.py:123` 用了 `Solid.Imprint`，但 `vba-official-reference.md` 全文 0 命中（它的 Solid 一节有 Add/Subtract/Intersect/Insert/Delete/BlendEdge/ChamferEdge，没有 Imprint） | 记为待核实项：**不能据此断言它是错的**（参考文档是第三方整理、覆盖不全），只是「查不到」 |
| 9 | **`_api_stats.py` 把守卫层算进 Mixin 方法数** —— 它报告 `cst_solver` 有 263 个类上公开方法，其中 `GuardState` 占 23 个，而 `GuardState` 根本不是 `setup` 的一部分 | 在 `docs/packages/cst_solver.md` 的统计口径里写明这个差异，避免下次又被这个数误导 |

---

## 5. 挂起与未验证项（下一轮要补）

| 项 | 为什么挂起 | 怎么补 |
|---|---|---|
| **5.1.3 两个模板不误报** | 需要真 CST（`TopoModeler` 会开 `tmp.cst`）。用户已要求**暂不启动新的 CST 计算** | 替代手段已做：用假 CST 对象的单测 + 对 `topo_modeler`/`topo_templates` 全部 `app.para` 调用点做了一次**静态审计**（确实只在 `feed.py` 的优化块命中，已由判据修正覆盖）。真机确认留到下次开 CST |
| **5.2.2 T3 普查的实测部分** | 同上 | 静态普查结论：本库**没有任何自动 save**，`save()` 永远是显式调用，所以 T3 只可能由用户脚本触发 —— 守卫给 `warning` 级提示是合适强度，不需要硬拦 |
| **`export_farfield_csv()`** | 本机没有任何**已存远场结果**的工程可读（阶段 4 已查明参考工程 `Result/` 只有 0.3 MB） | 下次跑完带远场监视器的算例后，用它读一次即可确认；失败时会抛 `ValueError` 并列出结果树候选路径 |
| **5.8 低频求解器族**（`EStaticSolver` 等） | 一个都不可能在无 CST 的情况下验证。阶段 4 的教训正是「从未被真实调用过的封装 = 8 处缺陷里 6 处的来源」 | **明确不做**，等有真实低频需求 + 能做 CST 验证时再排；不做比做错好 |

---

## 6. 出口判据

1. 守卫层在 `mode='off'` 下与改动前**逐字节一致**（回归基线）✅（有专门用例）；
   `mode='warn'` 下两个模板**不误报** —— ⏸ **只做了假对象 + 静态审计，真机未验**；
2. 反例测试能触发拦截，`cst_solver/tests/test_guards.py` 通过 ✅（43 passed）；
3. `docs/references/` 4 个文件 + `THIRD_PARTY_NOTICES.md` 就位，链接检查通过 ✅
   （`scripts/check_md_links.py` → 207 条相对链接全部可解析，exit 0）；
4. 三条文档旧账清零 ✅（并顺带修掉第 4 个同族缺陷与计数错误）；
5. `setup.pyi` 与 API HTML 已同步（铁律 §2）✅（225 个方法 / 24 个类别）。

> **结论**：出口判据 2–5 全部满足；**判据 1 的"模板不误报"只做到了离线等价验证**，
> 真机确认与 §7 的三项一起留到下次开 CST。**阶段 5 的离线部分视为完成。**

---

## 7. 明确不做

沿用采纳计划 §5 的边界：

- ❌ 依赖 `cst-runtime-cli`（CLI 形态、Python 3.13+、90% 功能重叠）
- ❌ 抄它的 113 个命令
- ❌ 抄它的 CLI 形态 / `bootstrap.py` 部署链
- ❌ 全库改造返回契约（会破坏 200+ 个方法签名，违反铁律 §3）
- ❌ 搬它的 disk marker / session 状态机（生命周期不同）
- ❌ 本轮不做低频/静电/静磁求解器封装（见 §7）
