# 阶段 4：验收与缺陷清账

> 状态：🔄 **进行中**（当前焦点）
> 对应旧编号：**新增阶段** —— 内容原本散落在旧 README §6「阻塞事项」+ 采纳计划批次 0 / 批次 5
> 上游：阶段 3 · 下游：阶段 5、6

---

## 1. 阶段目标与存在理由

06 为阶段 2/3 定的验收判据是**三条可量化的对比**：

| 判据 | 指标 |
|---|---|
| 几何 | 与参考工程 `AB_feed.cst` / `BA_feed.cst` 对比，**关键尺寸偏差 < 0.1%** |
| S 参数 | **谐振峰偏差 < 1 GHz** |
| 远场 | **主瓣偏差 < 5°** |

**这三条从未完整执行过。** 旧计划里阶段 3 的自测脚本从不调用 `build_all()`，
于是缺陷一路潜伏 —— 直到本轮第一次真跑，**接连暴露 8 处**（见 §2）。

> **本阶段的核心信条**：*「代码就绪」不等于「能用」。*
> 凡是「从未被真实调用过」的方法，单测、AST 扫描、存根同步**一个都查不出来**，
> 只有真跑一次才能发现。

---

## 2. 已完成（本轮实测记录）

### 2.1 构建已全程通过

用真实模板 `硅基\直波导\AB\tmp.cst` 的**副本**（先核对过它是干净模板：
`ModelHistory.json` 只有 3 条 —— 单位 + PML/边界，无几何）：

```
BUILD   messages: 空     ← 判据：跑通且无报错
REBUILD messages: 空     ← 判据：历史树无错重放
save()          成功     ← 产出 wg_AB_L18.cst（635 KB）
```

三组判据里的**第一组（几何）已完成可复现构建**，但**尚未做实体级比对**（见 §3 T1）。

### 2.2 参数级对比结果

本库实际定义/使用的 23 个参数中，**21 个与参考工程逐一吻合**：

| 判定 | 参数 |
|---|---|
| ✅ 一致 | `a` `h` `e1` `e2` `xup` `yup` `ydn` `x0` `wf1` `lf1` `lf2` `lf3` `wg_a` `wg_b` `wg_t` `fmin` `fmax` ＋ `p*` 路径参数 |
| ⚠️ 命名反转（已补偿） | `l1` ↔ `l2`（见 T3） |
| 🔴 实际差异 | 路径起点差一格（见 T2） |

规模对比：生成工程 81 参数 / 65 条历史，参考 77 参数 / 67 条历史 ——
差的 4 个正是本库新命名的 `p1x/p1y/p2x/p2y`（参考用旧命名 `px1/py1/px2/py2`）。

> ⚠️ **参数吻合只是代理指标，不能替代几何验证**：参数对不代表布尔序列、绕向、
> 阵列范围都正确。T1 才是本阶段的核心判据。

### 2.3 本轮暴露并已修掉的 8 处缺陷

| # | 位置 | 现象 | 修法 |
|---|---|---|---|
| 1 | `cst_solver/project.py::new_project()` | CST 2026 的 `DesignEnvironment.new_project()` **必传** `ProjectType`，缺参抛 `TypeError` | 加可选形参，默认 `ProjectType.MWS` |
| 2 | `cst_solver/project.py::save()` / `save_as()` | `Project` **没有** `save_as`，带路径保存必抛 `AttributeError` | 按真实签名 `save(path, include_results, allow_overwrite)` 改调 |
| 3 | `cst_solver/parameters.py::paras()` | docstring 声明支持 dict，**函数体未实现该分支** → CST 抛 `type must be array, but is object` | 按文档实现 dict → 两个等长数组的归一化 |
| 4 | `topo_modeler`（步骤缺失） | 流水线**从不定义材料**，模板又不带材料 → 第一条 extrude 报 `The specified material does not exist` | 新增 `builders/materials.py::build_materials()`，两个模板均调用 |
| 5 | `templates/straight_waveguide.py` | 阵列范围用路径推断值（`19/1/1`），**违反 ARCHITECTURE §6 硬约定 3**；且 `int(yup/2)=0` → CST 报 `Invalid number of repetitions` | 按参考改为 `xup=length+int(width/2)=25`、`yup=ydn=width=14` |
| 6 | `cst_solver/simulation/solver.py::T_solver()` | `Solver.Method` 只接受 `"Hexahedral"` / `"Hexahedral TLM"`，原写 `"T-Solver"` | 改为 `"Hexahedral"` |
| 7 | 同上 | `.Accuracy` / `.CalculateAllModes` / `.DetermineFreq` / `.DetermineFreqFromN` **都不是真实属性** | 删除，只保留实测可用属性；补齐 `set_steady_state_limit()` / `set_parallel_threads()` / `set_gpu_acceleration()` 三个封装 |
| 8 | `topo_modeler/builders/solver.py` | `_configure_solver_advanced()` 手写 VBA（违反 WORKFLOW §2），且 `.ParallelizationThreads` / `.GPUAcceleration` 实测不存在 | 改为调用 `cst_solver` 新封装 |

### 2.4 CST 事实核验结论（已入库，供后续复用）

**`Solver` 对象属性**（一个会话内逐条实测）：

| 可用 | 不可用（报 `10091 no such property or method`） |
|---|---|
| `Method` `SteadyStateLimit` `MaximumNumberOfThreads` `UseParallelization` `HardwareAcceleration` `MaximumNumberOfGPUs` `MeshAdaption` `FrequencySamples` `FrequencySampleRuleLin` `CalculateModesOnly` `SParaSymmetry` `FullDeembedding` `StimulationPort` `PBAFillLimit` | `Accuracy` `CalculateAllModes` `DetermineFreq` `ParallelizationThreads` `GPUAcceleration` |

**`.Repetitions`**：**接受** `int(14/2)`、`int(yup/2)` 这类整数表达式，
**不接受** `yup/2` 这种非整数表达式。

**面选取链路**（核验脚本 `scripts/verify_port_face_api.py`，全部通过）：

| 项 | 结论 |
|---|---|
| `model3d.Pick` 是否暴露 | ✅ 暴露，取到 `_cst_interface.RemoteObject` |
| 按坐标反查面编号 | ✅ `(0.5,0.5,1.0)` → 面编号 `1`（`int`） |
| `get_picked_count()` | ✅ 未拾取返回 `0`，非法 `kind` 抛 `ValueError` |
| 反查编号喂回 `pick_face()` | ✅ 已选面数 = 1，链路闭环 |
| `Coordinates "Free"` + 范围建端口 | ✅ 无报错 |

> 核验脚本：`scripts/verify_port_face_api.py`（新建空白工程，不触碰既有工程；
> 只关本次新建的 DE，**绝不碰用户既有 CST 会话**）。

### 2.5 T1 实体级几何对比结果（已完成）

**方法**：不打开 CST、不调 API —— CST 工程的 `Model/3D/ModelHistory.json`
保存着**全部建模 VBA**，每条实体创建命令都带**精确坐标与表达式**，离线逐实体对比即可。
工具：`scripts/compare_model_history.py`（本阶段新增，可复现）。

> 为什么不用 bounding box：CST 的 `Solid` 对象**没有**包围盒查询
> （官方 VBA 参考 §7 只有布尔/实体管理/网格属性/高级建模）；参考实现里的
> `show-bounding-box` 也只是**切换显示**（`Plot.DrawBox "True"`），不是查询。

**对比对象**：本库生成工程 `wg_AB_L18` vs 参考 `AB_feed`。

**结果一：26 个共有实体中，13 个几何逐字节一致**（忽略缩进空白）：

| 一致（13） | `g1A` `g2A` `g1B` `g2B` `tri_up_A` `tri_up_B` `tri_dn_A` `tri_dn_B` `feed1` `feed1_epc` `feed1_cut1` `wg1` `wg1_1` |
|---|---|

其中 `wg1`/`wg1_1`/`feed1_cut1`/`feed1_epc` 的 `Xrange`/`Yrange`/`Zrange`
与 `feed1` 的 9 个多边形顶点**完全逐字相同**。

**结果二：6 处「不一致」全部是两类可解释的等价**（非缺陷）：

| 表面差异 | 实质 | 判定 |
|---|---|---|
| `tri_up_A` / `tri_dn_A` / `tri_up_B` / `tri_dn_B` 的 `l1`/`l2` | 本库用 `l2` 的地方参考用 `l1`，反之亦然；而两库 `l1`/`l2` 的**取值本来互换**（本库 `l1`=0.65a、参考 `l1`=0.35a），折算后**孔尺寸逐一相同** | ✅ **等价**（即 T3 的答案） |
| `component1:wg1` / `component1:feed1` 的 mirror 中心 | 本库写 `p2x/2`，参考写 `x1*a/2`；两者数值均为 `4.365/2 = 2.1825` | ✅ **等价** |

**结果三：🔴 布尔运算序列差 4 条**（这是模型不等价的真正原因）：

| | add | subtract | mirror | **intersect** | **insert** | 合计 |
|---|---|---|---|---|---|---|
| 本库生成 | 7 | 4 | 2 | **0** | **0** | 13 |
| 参考 | 7 | 4 | 2 | **3** | **1** | 17 |

参考有、本库没有的 4 条：

```
vpcb Insert vpc_a_area1      ← VPC-B 里嵌入 A 区
vpca intersect vpc_a_area1   ← VPC-A 裁到 A 区
vpca intersect g1A           ← VPC-A 与晶体阵列求交
vpcb intersect g1B           ← VPC-B 与晶体阵列求交
```

**根因**：`topo_modeler/builders/` 里**已经实现了对应的两个函数**，但**零调用者**
（`grep intersect_crystal_with_vpc / intersect_vpc_with_substrate` → 除定义与
`__init__.py` 导出外无任何调用）：

| 函数 | 位置 | 调用者 |
|---|---|---|
| `intersect_vpc_with_substrate()` | `builders/vpc_region.py:55` | **无** |
| `intersect_crystal_with_vpc()` | `builders/crystal.py:151` | **无** |

也就是说：**本库根本没有把晶体阵列裁剪到 VPC 区域内**，两个 `g1A`/`g1B`
作为独立实体留在模型里。这与 T8（VPC 语义）是同一件事的两面 —— 现在它有了量化证据。

**结果四：实体命名分组不同**（`substrate`/`vpc_A`/`vpc_B` vs `vpca`/`vpcb`/`vpc_a_area1`），
属 T8 的构造路线差异，见该任务。

**T1 小结**：就「**共有实体的关键尺寸偏差**」而言，结论是 **0%**（13/13 逐字节一致），
判据 `< 0.1%` **满足**；但**整体模型尚不等价** —— 差在那 4 条布尔运算，属 T8。

---

## 3. 任务清单

> **状态约定**：标 ✅ 的已完成项**已从[总纲](../README.md) §4 的待办清单移出**，
> 此处保留其结论与证据；仍在待办的只剩 **T5 / T7 / T10**
> （T4、T6、T9 已完成，详见各自小节）。
>
> **本轮遇到的 10 个问题与更正**（含 `Insert` 语义纠错、VPC-A/B 反了、
> 基板半宽只有 1 行、`MaximumNumberOfGPUs 0` 非法、导入顺序、
> "S 参数与监视器无关"的更正等）集中记录在[总纲 §4.4](../README.md)，
> 避免在多处重复维护。

### T1 ✅ 实体级几何对比（已完成，见 §2.5）

**结论**：26 个共有实体中 13 个几何**逐字节一致**，关键尺寸偏差 **0%**（判据 < 0.1% **满足**）；
6 处表面差异经核对均为**等价**（`l1`/`l2` 命名互换、mirror 中心表达式不同但同值）；
但**整体模型尚不等价** —— 差 4 条布尔运算，归入 T8。

### T2 路径起点差一格：决策 + 修正（🟡 需决策）

| | 起点 | 终点 |
|---|---|---|
| 本库 `p1x` / `p2x` | `-0.2425`（= **−a**） | `4.365`（= 18a） |
| 参考 `px1` / `px2` | `0` | `4.365`（= 18a） |

终点一致，但本库起点早了一个晶格常数：本库路径 `c ∈ [-1, 18]`（19 格），参考 `c ∈ [0, 18]`（18 格）。
**后果**：基板与晶体阵列在 x 方向**比参考长一格**。

**成因**：`StraightWaveguide.__init__` 的 `start(0, -1)` 配 `.move(length + 1, 'c')`；
原注释只校验了终点（"终点 c = -1 + (length+1) = length"），**没校验起点**。

**待决策**：改成 `start(0, 0) + move(length, 'c')` 与参考对齐，还是起始多一格是有意为之（例如为馈源留位）？
**改前必须确认** —— 它会整体改变模型长度。

### T3 ✅ `l1`/`l2` 按实体核对（已完成，结论：补偿正确）

参考历史树的真值是 `tri_up_A→l1`、`tri_dn_A→l2`、`tri_up_B→l2`、`tri_dn_B→l1`，
而两库的 `l1`/`l2` **取值互换**（本库 `l1`=0.65a 大孔、参考 `l1`=0.35a 小孔）。
实体级对比（§2.5）给出的答案：

| 实体 | 本库用的参数 | 参考用的参数 | 折算后孔尺寸 | 判定 |
|---|---|---|---|---|
| `tri_up_A` | `l2` = 0.35a | `l1` = 0.35a | 相同（小孔） | ✅ |
| `tri_dn_A` | `l1` = 0.65a | `l2` = 0.65a | 相同（大孔） | ✅ |
| `tri_up_B` | `l1` = 0.65a | `l2` = 0.65a | 相同（大孔） | ✅ |
| `tri_dn_B` | `l2` = 0.35a | `l1` = 0.35a | 相同（小孔） | ✅ |

**结论**：`crystal.py` 的补偿**完全正确**，四个三角形的孔尺寸分配与参考逐一对应。
`l1`/`l2` 只是**命名约定不同**，不是缺陷 —— 但**对比时永远不能按名字比**。

### T4 核验 `_create_farfield_monitor()`

它仍手写 `With Farfield` VBA（违反 WORKFLOW §2），正确性**未验证** ——
因为默认 `monitors=('E',)` 不走该分支。

**怎么做**：实测正确的远场监视器 VBA（大概率应是 `Monitor` 对象 + `.SetDimensionType "Farfield"`），
核验后给 `cst_solver` 补封装，再回来改 builder。

### T5 `unit_antenna` 同源隐患

`unit_antenna.py:122` 用同一套 `path.get_array_range()`，与已修的 `StraightWaveguide` **同源**。
**未验证、未修改** —— UnitAntenna 是含拐弯的路径，需先确定其参考基准再决定阵列范围取值。

### T6 ✅ S 参数对比（已完成，见[总纲 §4.5](../README.md)）

时域求解（关自适应 + 稳态 −20 dB），两模型各跑一次，比较 4 个频点：
**8 个点里 7 个在 1 dB 内**；S21（传输）4 点全部 ≤ 0.73 dB，S11 仅 350 GHz 一处差 1.83 dB。

**两点局限**（写清楚，避免过度解读）：
1. 1 dB 是**代理容差**，不是原判据 —— 原判据是「谐振峰偏差 < 1 GHz」（峰值位置对比），
   需要先做峰值识别；用少数频点的 dB 差代理它只能发现结构性错误；
2. S11 在反射低谷附近对微小差异极敏感，1.8 dB 的差可能是数值/网格差异而非结构错误，
   判定需要看曲线形状而非单点数值。

**成本记录**：单次时域求解约 5300 秒 CPU、峰值内存约 2.4 GB（省时配置下）。
工具：`scripts/compare_s_parameters.py`（参考工程自动用副本，绝不改原件）。

### T7 远场主瓣偏差 < 5°（⏸ 暂挂）

依赖 T4 的 CST 验证；用户已要求**暂不启动新的 CST 计算**，故挂起。

**风险**：时域求解耗时不可控（本轮实测单次约 5300 秒 CPU），**启动前必须先给用户预估**。

### T8 VPC 区域语义决策（🟡 需决策）

参考 `AB_feed.ipynb` 的 VPC-A 是「三角形区域 + `insert` / `intersect`」造出来的，
而本库 `build_vpc_regions()` 实现的是「路径上/下半区带状区域」。**两者不是同一个几何**；
且参考 notebook 里 VPC-A 实际落在 **`y < 0` 半平面**（`vpca_up/dn` 的 up/dn 指**三角形朝向**，不是上下半区）。

本轮修 CCW 绕向时把拐弯路径的带状区域改成了「真正沿路径的带」（顶点数 `N+3 → 2N+1`），**使差异更明确**。

**待决策**：以参考工程为准复刻旧造法，还是以新带状语义为准并重新定基线？

**⚠️ 本轮已量化（见 §2.5 结果三）**：除造法不同外，参考还有 **4 条本库没有的布尔运算**：

| 参考的操作 | 本库 |
|---|---|
| `vpcb Insert vpc_a_area1` | 无 |
| `vpca intersect vpc_a_area1` | 无 |
| `vpca intersect g1A` | 无 |
| `vpcb intersect g1B` | 无 |

而 `builders/` 里**已经写好**了对应的两个函数却**零调用者**：
`intersect_vpc_with_substrate()`（`vpc_region.py:55`）、
`intersect_crystal_with_vpc()`（`crystal.py:151`）。

**直接后果**：本库**没有把晶体阵列裁剪到 VPC 区域内**，`g1A`/`g1B` 作为独立实体留在模型里。
这意味着当前生成工程与参考工程**不等价**，S 参数对比（T6）在此之前没有意义。

**决策时必须一并回答**：这 4 条是否要补进 `build_all()`？
- 若「复刻旧造法」→ 直接调用那两个既有函数，并调整顺序（参考是先 `intersect` 再 `add feed1`，本库目前先 `add`）；
- 若「以新语义为准」→ 需说明新语义下晶体如何与 VPC 关联，并**重新定一个可验证的基线**（不能只说"更合理"）。

#### T8/T11 ✅ 已决：**以参考工程为准**，并已实施（2026-09-15）

用户决策：**以参考工程为准**。实施过程中又查明三件事（都写进了代码注释与 ARCHITECTURE §6）：

**（1）`Insert` 的语义此前被记错了 —— 是差集不是并集**

CST 官方帮助 `common_vbasolido_solid_object.htm` 原文：

```
Insert ( solidname solid1, solidname solid2 )
    Performs an subtraction between the solids solid1 and solid2 (solid1 - solid2)
    but does not delete solid2.
```

第三方整理的 `vba-official-reference.md` 记成 `s1 ∪ s2` 是**错的**。纠正后参考链条自洽：
`vpcb Insert vpc_a_area1` → vpcb = 面板 − 下半梯形 = **上半区**。

**（2）参考的 VPC-A 是下半区，本库原来写反了**

两条独立证据：`vpc_a_area1` 多边形为 `y ∈ [ymax_dn, 0]`；且 `vpca intersect vpc_a_area1`
（Intersect 结果留第一个操作数）→ vpca 即下半区。即参考的配对是**晶体 A ↔ 下半区**。
本库原为「VPC-A = 上半区」→ 整个器件在 y 方向镜像。**已改为 A = 下半区。**

**（3）🔴 又发现一处更根本的缺陷：基板/VPC 半宽只有 1 行晶格**

| | y 半宽 | 折合行数 |
|---|---|---|
| 本库原值（`y_margin='e2'` 默认） | 0.21 | **1 行** |
| 参考（`ymax_up = e2*y1`） | 2.94 | **14 行** |

而晶体阵列按 `yup=ydn=14` 铺满 14 行 —— 阵列远在基板之外。
根因：`width` 参数**只被用来算阵列范围**，没用来定基板/VPC 宽度。

**实施清单（3 处，已提交）**：

| # | 改动 | 位置 |
|---|---|---|
| 1 | VPC-A = 下半区、VPC-B = 上半区 | `builders/vpc_region.py` |
| 2 | `intersect_crystal_with_vpc()` 操作数顺序改为保留 VPC 名（对齐参考 `vpca intersect g1A`） | `builders/crystal.py` |
| 3 | `y_margin = f'{width}*e2'`；补晶体∩VPC 求交；去掉多余 `substrate` | `templates/straight_waveguide.py` |

关于第 3 点的「去掉 substrate」：参考工程实体清单**没有 substrate**，它的硅就是
`vpca`（= 下半区∩晶体A + feed1 + 上半区∩晶体B），晶体经求交裁剪成**图形化**的硅。
本库若额外保留一块整幅未图形化的 substrate（同为 Silicon (lossy)），
求并会**把光子晶体孔洞填平**。故本模板改为「两个 VPC 区域即硅本体」；
`build_substrate()` 仍保留在库中（`unit_antenna` 等其它器件可能仍需要）。

**实施后复核（`scripts/compare_model_history.py` + 布尔序列对比）**：

| 指标 | 改前 | 改后 | 参考 |
|---|---|---|---|
| 布尔/变换条数 | 13 | **15** | 17 |
| `intersect` | 0 | **2** | 3 |
| `insert` | 0 | 0 | 1 |
| 实体数 | 40 | 37 | 38 |
| 共有实体逐字节一致 | 13/26 | **13/26** | — |

布尔序列现在与参考**除 VPC 面板构造那 2 条外一一对应**（`vpcb Insert vpc_a_area1`、
`vpca intersect vpc_a_area1`）——那 2 条在本库是「直接建成上/下半区」，属构造路径不同、
**结果等价**（已核对多边形：本库 `vpc_A` = `y ∈ [p1y−14e2, p1y]` = 参考的下半区）。

**仍不满足等价的一点**：路径起点差一个晶格周期（**T2**）——
本库 `vpc_A/vpc_B` 的 x 起点是 `p1x = −0.2425`，参考是 `0`。

### T9 面编号方案落地

方案已定（双通道）、事实已验（§2.4），剩下实现：

| 端口面类型 | 方案 |
|---|---|
| 轴对齐矩形面（直波导两端、天线入口） | `create_waveguide_port_free(...)`（`Coordinates "Free"` + `Xrange/Yrange/Zrange`） |
| 非轴对齐面（拐弯 / 扭转后的端面） | 正算出面上一点 → `get_face_id_from_point()` → `pick_face()` + `add_port()` |

**改动**：`topo_modeler/builders/port.py` 的硬编码 `'10'` / `'22'`。

### T10 `templates` 包名风险

顶层包名过于通用，安装进 site-packages 有与第三方包重名风险。
计划改名 `topo_templates`，并在一个版本周期内保留旧名 shim。

---

## 4. 出口判据

1. **三组验收判据全部有可复现的实测结论**（几何 / S 参数 / 远场）；
2. **T2、T8 两个决策有明确结论**并落到文档；
3. 其余 TODO 要么完成，要么明确标注「不做」并给出替代方案；
4. `docs/ARCHITECTURE.md` §6 与 `skills/developer/cst-solver-dev.md` 的新硬约定已同步。

---

## 5. 已建立的验收工具链

| 工具 | 用途 |
|---|---|
| `scripts/verify_port_face_api.py` | 面编号反查与端口 Free 模式的**事实核验**（V2/V3/V4） |
| 模板副本工作流 | 一律用 `tmp.cst` 的**副本**验证，**绝不改原件** |
| DE 基线清理模式 | 开跑前记录 `running_design_environments()` 基线，收尾只关**基线之外**新出现的 DE，绝不碰用户既有会话 |

> ⚠️ **CST 会话纪律**（本轮教训）：脚本收尾若走 `app.close()`，而 `cst_file` 尚未建立，
> `close()` 会因 `AttributeError` 失败并**留下一个 CST 窗口**。
> 正确做法是「基线比对 + 兜底 connect/close」，见 `verify_port_face_api.py::_cleanup()`。
