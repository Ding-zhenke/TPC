# 阶段 7：工具层 + 报告引擎 + 审计落盘

> 状态：🔄 **进行中** —— **「看得见 / 留得下」两半已完成并离线验收通过**
> （配置 ▸ 结果读取 ▸ 报告 ▸ 审计）；**「跑得动」那一半（扫描 / 批量 / GA 桥接）未做**。
> 对应旧编号：**06 的阶段 5**（⚠️ 在 05 里叫「阶段 6–7」）＋ `cst-runtime-cli` 采纳计划**批次 4**
> 上游：阶段 6 · 下游：阶段 8
> 模块级详细规格见 [`../06_整合实施计划_分阶段分模块.md`](../06_整合实施计划_分阶段分模块.md) §七

---

## 1. 阶段目标

让仿真**跑得动、看得见、留得下**：

- **跑得动** —— 结果自动读取、参数扫描、批量建模、GA 优化
- **看得见** —— 报告引擎（原 `plot_results()` 抛 `NotImplementedError`）
- **留得下** —— 审计落盘

**本轮完成的是后两半 + 结果读取**；「跑得动」里的扫描/批量/优化需要一个能真跑 CST 的
执行器，按当前约束（暂不启动新的 CST 计算）本轮不做 —— 见 §7。

## 2. 现状

| 项 | 现状 |
|---|---|
| `topo_modeler/config.py` | ✅ **已建**（模块 5.2）：YAML 配置驱动 + **可执行取值域** |
| `topo_modeler/result_reader.py` | ✅ **已建**（模块 5.1）：`ResultReader` |
| `topo_modeler/report.py` | ✅ **已建**（§3.2）：自包含 HTML 报告引擎（零 CDN / 零 JS） |
| `topo_modeler/audit.py` | ✅ **已建**（§3.3）：审计落盘 |
| `TopoModeler.read_results()` / `plot_results()` | ✅ **已接线**，不再抛 `NotImplementedError` |
| `topo_modeler/scanner.py` / `batch.py` / `optimizer.py` | ❌ **未建** |
| `tpc_toolkit/ga_optimizer.py` | 已存在（纯 GA 算子）；阶段 7 的 `optimizer.py` 只做**与 CST 的桥接** |
| 全仓 `import yaml` | ✅ 现在有（`config.py` 惰性导入；PyYAML 是可选依赖） |
| 审计落盘 | ✅ 有（`logs/tool_calls.jsonl` + `production_chain.md` + `params/` 存档） |

> **放哪个包**：按 06 §七 的目录结构，五个工具模块都放在 **`topo_modeler/`**
> （不是 `tpc_toolkit/`）—— 因为它们要读 CST 结果 / 建模板，而
> `tpc_toolkit/` 的立包前提是「**不依赖 CST**」。`optimizer.py` 的定位也是
> 「把 `tpc_toolkit/ga_optimizer.py` 的算子与 CST 接起来」，仍然属于 `topo_modeler/`。

---

## 3. 本轮交付

### 3.1 `ResultReader`（模块 5.1）✅

`cst_solver.Result` 的**批量化 + 可视化**外壳（**不**重复造读取轮子，阶段 5 已把底层做完）。

统一了三件事：

1. **批量**：一次读回全部 S 参数，不用手写循环；
2. **统一口径**：频率一律 `float`、S 值一律 `complex` 或 dB ——
   **不把 CST 的复数外壳漏给调用方**（阶段 4 T6 踩过：
   CST 的 `xdata/ydata` 都是 complex，强转会 `ComplexWarning` / `TypeError`；
   本轮在报告引擎里又踩了一次，见 §8 第 2 条）；
3. **出图 / 出报告**：转交 `report.py`。

关键 API：`read_s_parameters()` / `read_s_parameters_db()` / `value_at()` /
**`peak_position()`** / **`peak_shift()`** / `export_csv()` / `plot_all()`。

> 🔑 **`peak_shift()` 把阶段 4 的原始判据变成了可执行的东西**：
> 「谐振峰偏差 < 1 GHz」此前只是文档里的一句话，没有任何 API 支撑；
> 现在 `ResultReader(a).peak_shift(ResultReader(b))` 直接给出频差。

**可注入**：`ResultReader(source)` 的 source 可以是 .cst 路径，也可以是任何实现了
`read_all_s_parameters()` 的对象 —— 后者让核心逻辑**完全可离线验收**（测试用假对象，
不必造一个含结果的工程）。

### 3.2 报告引擎（§3.2）✅

`HtmlReport` + 4 个内联 SVG 图元：`svg_line_chart` / `svg_heatmap` / `svg_polar` / `svg_timeline`。

**硬约定：自包含**——生成的 HTML 里**没有**任何 `http(s)://` 外链、`<script>`、`<link>`，
所有图形都是内联 SVG（浏览器原生渲染）。有测试逐条守着。这样断网能开、十年后能开、
能直接进 git / 邮件。

> ⚠️ **计划里的「3D 远场（WebGL）」明确不做**：手写 WebGL 必须内联 JS，
> 既违背零 JS 约定、又无法离线验收。远场用**二维极坐标**（`add_polar`，自动标出主瓣峰值方向），
> 看主瓣/旁瓣足够。这是**有意偏离计划**，不是漏做。

`add_audit(audit)` 能把审计记录直接渲染成「各步骤耗时 + 失败步骤」小节 ——
于是**一份报告同时回答「结果长什么样」和「这次是怎么跑出来的」**。

### 3.3 审计落盘（§3.3）✅

`AuditLog`：

```
logs/
├── tool_calls.jsonl          # 一行一个 JSON 事件（机器读）
├── production_chain.md       # 人读的流程链（含「失败步骤」「最慢的 10 步」）
└── params/<name>-<时间戳>.json   # 参数存档，**带微秒 + 冲突兜底，绝不覆盖**
```

- `with audit.stage('build_all'):` 自动记开始/结束/耗时/异常（异常照常抛出，不吞）；
- **审计失败不拖垮主流程**：默认 `strict=False`，写盘出错只记 `last_error`，
  `strict=True` 才抛 —— 与库内其它辅助设施（守卫探针、几何自查）同一态度；
- 目录可用环境变量 `TPC_AUDIT_DIR` 覆盖。

### 3.4 YAML 配置驱动（模块 5.2）✅ + 取值域缺口补齐（§4.1）✅

`load_config` / `validate_config` / `template_from_config` / `modeler_from_config` /
`example_config` / `dump_config` + `validate_only=True`（**不碰 CST** 的预检通道），
取值域见 §4.1。

### 3.5 里程碑 ✅

计划写的里程碑是 `model.build_all().save().run().plot_results()` 一条链。
`plot_results()` 现在真能用了：

```python
modeler.plot_results('report.html', audit=audit)   # 自包含 HTML（含审计小节）
```

---

## 4. 规格缺口：本轮补齐了什么

### 4.1 YAML 配置的取值域（06 §14.3）✅

原来是 `model.type` 三个取值加省略号 —— 省略号意味着**没有可执行的校验**。现在：

**`model.type` 完整枚举**（与 `topo_templates/` 类名一一对应）：

| 取值 | 类名 | 状态 |
|---|---|---|
| `straight_waveguide` | `StraightWaveguide` | ✅ 已实现 |
| `unit_antenna` | `UnitAntenna` | ✅ 已实现 |
| `grin_lens_antenna` | `GRINLensAntenna` | 计划中（阶段 6 剩余项） |
| `multiport_antenna` | `MultiPortAntenna` | 计划中（阶段 8） |
| `power_divider` | `PowerDivider` | 计划中（阶段 8） |
| `mzi_switch` | `MZISwitch` | 计划中（阶段 8） |

计划中的取值**语法上合法**（配置可以先写出来、结构可以先校验），
但会在 `template_from_config()` 里明确抛「属于哪个阶段、类名叫什么」，
**不会静默变成别的模型**。

**其余字段**的合法取值、单位、范围、默认值全部落在 `config.FIELD_SPECS` 里，
可用 `config.field_help('straight_waveguide')` 逐行打印：

```
[geometry]
  lattice_constant     float [mm] > 0 — 三角晶格常数 a
  topology             enum ∈ {AB | BA} — 拓扑相
  length               int [格] ≥ 1 — 波导长度（晶格数，不含起点偏移）—— 仅 straight_waveguide
  ...
[solver]
  monitors             str_list — 监视器列表，取值 E | H | Farfield
```

**已补的几条关键约定**（原计划没写）：

- `topology` 只接受 `AB` / `BA`（**大写**，`ab` 会被拒）；
- `monitors` 取值就是 `E` / `H` / `Farfield`（来自
  `cst_solver/simulation/monitors.py` 的 `_FIELD_CONFIG`），且**必须是列表**——
  YAML 里写 `monitors: E` 会被明确拒绝并提示写 `[E]`（这是最容易写错的一处）；
- 长度一律 **mm**、频率一律 **GHz**、长度/宽度类晶格数是**整数**；
- `feed.type` / `output.path` / `solver.fmin+fmax` 三处与模板构造参数名不同，
  由 `FIELD_SPECS.ctor` 统一说明（不会出现「文档写 A、签名是 B」的漂移）；
- **字段还要与模板签名对表**：给直波导写 `bend_angle` 会报错并指出
  「那是 unit_antenna 的字段」；
- 接受的字段集合是**从模板构造签名自动推导**的，不维护第二张清单。

### 4.2「验证标准」不可测量（06 §14.4）—— ⚠️ **原记录不准，已重新定性**

计划原文写「8 处写了『优化 ……』却没有判据，如 `06:542/560/578`、`06:753`」。
**逐条核对后不成立**：

- `06:542` 是一段 YAML 示例；
- `06:753` 是**「被**优化掉的旧代码清单」—— 完全另一个意思；
- `06:560/578`（ParameterScan / BatchModeler 的「验证标准」）确实是问题，
  但问题是**「扫描结果合理，热力图正确」「3 个模型批量建模成功，对比图正确」
  这类说法不可测量**，而不是「优化 X」。

**已改写成可执行判据**（见 §5.2），并把原措辞记在这里以免下次又按错的描述去找。

---

## 5. 出口判据

1. 五个模块至少 `ResultReader` / `Config` / `ParameterScan` 落地并有最小可运行示例
   （`tpc_toolkit` 的验收标准同样适用：不引入 CST 依赖的部分要写明数组形状/dtype）；
   - `ResultReader` ✅、`Config` ✅、`ParameterScan` ❌ **未做** ⇒ **本条未满足**
2. 报告引擎能从真实仿真结果生成**自包含 HTML**（无外部 CDN）；✅
   （用阶段 4 T6 的真实实测数据做过端到端演示；自动化测试逐条检查无外链/无 JS）
3. 审计落盘能回答「这次模型是怎么建出来的」；✅
   （`production_chain.md` 含步骤表 + 失败步骤 + 最慢 10 步；参数存档不覆盖）
4. YAML 取值域与「优化」判据补齐；✅（YAML 见 §4.1；§4.2 的判据已改写，见 §5.2）
5. 并发模型有明确结论。✅（阶段 5 §5.7.2：按证据强度分三档写在
   `skills/developer/cst-solver-dev.md`；BatchModeler 必须**串行**、
   进循环前记 DE 基线、只关自己开的）

> **结论**：判据 2–5 满足；**判据 1 只完成 2/3**（缺 `ParameterScan`）。

---

## 5.2 改写后的可执行判据（替代「合理 / 正确」）

| 模块 | 原「验证标准」 | 改写为 |
|---|---|---|
| ParameterScan | 扫描结果合理，热力图正确 | 每个参数组合产生**唯一**的标识（不撞名、可复现）；结果聚合后每个格子都能追溯到具体的参数组合与 .cst 路径；热力图行列标签与扫描轴**一一对应**（断言 `len(x_labels) == len(轴1)`）；导出的 CSV 行数 = 组合数 |
| BatchModeler | 3 个不同模型批量建模成功，对比图正确 | 每次 `setup()` 都配一次 `close()`（可用 DE 数量断言：跑完后 `running_design_environments()` 回到基线）；**串行**（同时最多 1 个 DE）；单个模型失败不终止整批且失败项列入汇总；对比图的系列名 = 模型名 |
| GeneticOptimizer | 优化后目标函数值优于初始值 | 用**可复现的随机种子**；记录每代最优值曲线（进审计）；末代最优 ≥ 初代最优（固定种子下**必然**成立，不是「看起来更好」）；评估次数 = `population × generations`（可用计数桩断言） |

---

## 6. 明确没做的两件事（有意偏离）

1. **3D 远场 WebGL**（见 §3.2）—— 违背零 JS 约定且无法离线验收；改为二维极坐标。
2. **`scanner` / `batch` / `optimizer` 三个模块**（见 §7）——
   它们的核心价值在「真的把仿真跑起来」，而那需要 CST。

---

## 7. 未做与未验证（下一轮）

| 项 | 为什么 | 怎么补 |
|---|---|---|
| `scanner.py`（ParameterScan） | 需要能真跑 CST 的执行器。**但纯逻辑部分（网格枚举、结果聚合、热力图行列对齐、CSV）不依赖 CST** | 用「注入执行器」（`run_fn(config) -> results`）先做；测试用桩函数。审计与报告已就绪，可直接接上 |
| `batch.py`（BatchModeler） | 同上；且并发模型虽已有结论，仍需真机确认许可/多实例上限 | 先做**串行**版本（结论已经明确要串行）+ DE 基线清理；并发上限留待真机 |
| `optimizer.py`（GA 桥接） | 同上；且 `tpc_toolkit/ga_optimizer.py` 已有的算子需要被真实目标函数驱动 | 桥接层可用注入的目标函数先做，把 `ga_optimizer` 接起来 |
| `ResultReader` 真读 .cst | 本机**没有含结果的工程**（阶段 4 已查明参考工程的 `Result/` 只有 0.3 MB），造一个要跑约 5300 s CPU | 下次跑完任何算例后跑一次 `ResultReader(path).read_s_parameters()` 即可确认 |
| 报告的 3D 远场 | 见 §6 | 若确有必要，再单独立项（需接受内联 JS） |

---

## 8. 本轮遇到的问题与更正（留痕）

| # | 问题 | 处理 |
|---|---|---|
| 1 | **`time.time()` 在 Windows 上粒度约 15 ms** —— 同一刻写的两条审计记录 `epoch` **完全相同**（实测），原实现用 `epoch` 排序等于没排；用 `(ts, tool, status)` 去重还会把「同一秒 + 同名步骤」误判成重复 | 改为**以文件追加顺序为准**（JSONL append-only ⇒ 文件顺序就是时间顺序），去重改用每条记录的 `uid`；老记录退回内容四元组（并去掉 `epoch`） |
| 2 | **报告引擎在复数 S 参数上直接崩** —— `_split_xy` 对第二列做了 `float()`，而 CST 返回的是 complex ⇒ `TypeError`。这是**同一个坑第二次**（阶段 4 T6 是 `ComplexWarning`） | `_split_xy` 不再强转第二列，复数交给 `_to_db()` 取模值；补了专门用例 |
| 3 | **参数存档会覆盖** —— 文件名时间戳只到秒，同一秒两次 `archive_parameters()` 撞名覆盖，而「存档不覆盖」是对外承诺 | 时间戳加微秒 + `_unique_path()` 冲突兜底（`-2`/`-3`…），并补了「两次存档得到两个文件且内容各自正确」的用例 |
| 4 | **时间线在「所有步骤耗时 < 1 ms」时谎报「没有数据」** —— 原写法用 `if r.get('duration_s')` 过滤，`0.0` 是假值 | 改为判 `is not None`；全为零时照画（条宽用最小值）并在图下注明 |
| 5 | **`validate_config` 对「计划中的模型类型」直接报错** —— 类还不存在 ⇒ 拿不到签名。但这与「语法上合法、建实例时才说未实现」的设计相矛盾 | 计划中的类型改为**放行语法校验**并附 `_warnings`（「字段未与模板签名核对」），真正的「未实现」由 `template_from_config()` 抛，消息里带阶段与类名 |
| 6 | **06 §14.4 的「8 处优化」记录不准**（见 §4.2） | 重新定性为「验证标准不可测量」并改写判据；原文留在此处备查 |
