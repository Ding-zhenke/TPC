# 阶段 6：复杂模板层（GRIN 透镜）

> 状态：🔄 **进行中** —— **几何层（`LensBuilder` 的几何部分）已完成并离线验收通过**；
> CST 建模步骤已收编但**未在真机验证**；`GRINLensAntenna` 模板未做。
> 对应旧编号：**06 的阶段 4**（⚠️ 在 05 里叫「阶段 5」；04 里叫「阶段三（部分）」）
> 上游：阶段 4（几何验收通过）、阶段 5 · 下游：阶段 7
> 模块级详细规格见 [`../06_整合实施计划_分阶段分模块.md`](../06_整合实施计划_分阶段分模块.md) §六

---

## 1. 阶段目标

从「两个基础器件」扩展到「带透镜的复杂器件」：
`LensBuilder` + `GRINLensAntenna` 模板。

---

## 2. 现状（开工前必须知道）

| 项 | 现状 |
|---|---|
| `builders/lens.py` | ✅ **已建**（2026-09-15）：`GrinLensSpec` / `GrinLensHoles` / `build_grin_lens_holes()` / `build_grin_lens()`，已在 `builders/__init__.py` 导出 |
| `TopoModeler.build_lens()` | ✅ **已接线**，不再抛 `NotImplementedError`（几何 + DXF + CST 步骤一条龙） |
| 回归测试 | ✅ `topo_modeler/tests/test_lens.py`（36 项）：与原脚本 `lens_build.py` **逐点等价** + CST 调用序列**逐条等价** |
| `topo_modeler/lens_build.py`、`lens_build_standalone.py` | 仍是**脚本**（保留不动：notebook 仍在 exec 它）；库侧不再依赖它们 |
| `GRINLensAntenna` 模板 | ❌ **不存在**（本阶段剩余工作） |

> `lens_build_standalone.py` 是**唯一**调用过 `app.new_material('Silicon (lossy)')` 的地方 ——
> 阶段 4 补的 `build_materials()` 把它正式收编进流水线了。

---

## 3. 任务清单

### 3.1 LensBuilder（模块 4.1）—— 几何部分 ✅ / CST 部分 ⏸

两种构造方式：

| `method` | 做什么 | 状态 |
|---|---|---|
| `'hexagon'` | 按六边形晶格生成孔阵列（GRIN 渐变） | ✅ **已实现**（`build_grin_lens_holes`），公式见 §4 |
| `'dxf'` | 从 DXF 导入透镜轮廓 | ⏸ **未做** —— 与 `'hexagon'` 共用 `build_grin_lens()` 的后半段（导入 → 镜像 → 椭圆减孔 → 楔形裁剪），只差一个入口函数 |

### 3.2 GRINLensAntenna 模板（模块 4.2）—— ❌ 未做

组装 `TopoModeler` + 透镜 + 参数 + 端口 + 求解器，一个类 = 一个器件。

---

## 4. ⚠️ 开工前必须补齐的规格缺口（06 §14.1）—— ✅ **已补齐（hexagon）**

06 对这两块**只给了「做什么」，没给「怎么做」和「怎么验证」**。现已补齐如下
（**全部来自可运行的代码，不是推测** —— 见 `topo_modeler/builders/lens.py` 的 docstring
与 `topo_modeler/tests/test_lens.py` 的等价性回归）。

### 4.1 `method='hexagon'` 的半径-距离公式 ✅

**坐标系**：局部系，原点 = **椭圆近焦点** = **大六边形 0° 顶点**。

| 量 | 公式 | 单位 | 参考配置取值 |
|---|---|---|---|
| 六边形半径 | `hex_size = a / √3 / ratio` | mm | 0.0175003125 |
| **孔网格格距** | `a2 = hex_size · √3` = **`a / ratio`** | mm | 0.0303125 |
| 长半轴 | `ec_a = nx · a2` | mm | 1.151875 |
| 短半轴 | `ec_b = ny · a2 · sind(60°)` | mm | 0.892547432 |
| 离心率 | `e = √(ec_a² − ec_b²) / ec_a` | — | 0.632126955 |
| 焦距（椭圆中心） | `ec_c = e · ec_a` | mm | 0.728131237 |
| **渐变区起点** | `d0 = 6 · hex_size · 2 · √3` = **`12 · a2`** | mm | 0.36375 |
| 内圈孔半径 | `r1 = r1_0 / ratio` | mm | 0.012625 |
| 外圈孔半径 | `r2 = r2_0 / ratio` | mm | 0.015325 |

**孔心筛选**：椭圆包络判据 —— 到**两焦点**距离之和 `≤ 2·ec_a·tol`
（焦点在原点与 `(2·ec_c, 0)`）。`tol` 默认 **1.03**（参考实现用 1.1）：
`>1` 是为了把刚压在边界上的孔也收进来，否则椭圆最扁的两头会留下一圈没孔的实心边。

**孔半径**：沿椭圆**等高线**距离 `d_ell = √(x² + y²/(1−e²))`，
在 `[d0, d_out]` 上线性插值到 `[r1, r2]`：

```
t     = clip((d_ell − d0) / (d_out − d0), 0, 1)
r(x,y)= r1 + (r2 − r1) · t
```

**`d_out` 的确切含义**（原来完全没写清，两种语义差别很大）：

| 取值 | `d_out` | 后果 |
|---|---|---|
| `'ec_a'`（**默认，原脚本的取值**） | `ec_a` | `d_ell` 实际范围是 `0 → (ec_c + ec_a)`，分母只到 `ec_a` ⇒ **外侧约 63% 的孔全是 `r2`**，孔半径只在前 37% 单调渐变 |
| `'shift_plus_ec_a'` | `ec_c + ec_a` | 孔半径沿**整条**透镜单调渐变到底 |

> 原脚本注释里就写着这一点（并提示「想全长度渐变就改掉」），但**默认行为**是前者
> —— 本库把它固化成显式参数 `d_out_mode`，默认与原脚本一致（有等价性回归钉住）。

**层数定义**：孔网格是**点型六边形交排网格**（`HexGridVisualizer`，
`orientation='pointy'`、`coord_type='offset_q'`），行范围 `(-ny, ny)`；
**列范围不能取 `(-nx, nx)`** —— 见下。

🔴 **必须记住的坑：列范围按椭圆真实 x 范围取。** 局部系里近焦点在原点，
椭圆 `x ∈ [ec_c − ec_a, ec_c + ec_a]` **关于 0 不对称**，所以

```
col_min = floor((ec_c − ec_a) / a2) − 1
col_max = ceil ((ec_c + ec_a) / a2) + 1
```

原脚本注释记着：旧版写 `(-nx, +nx)`，网格只到 `x = nx·a2`，而椭圆要伸到
`ec_c + ec_a` ⇒ **椭圆外侧整片没孔 = 实心硅**。本库的
`test_coverage_check_catches_missing_columns` 就是这条的反例测试。

**验收判据**（三道，全部与 CST 无关）：`GrinLensHoles.run_self_checks()`

| 自查 | 判据 | 参考配置实测 |
|---|---|---|
| 裁剪不重叠 | 楔形裁剪后透镜与大六边形的重叠面积 `< 1e-3 mm²` | 0.00000000 mm² |
| 孔阵覆盖 | 透镜内采样点到最近孔心的最远距离 `d_max ≤ 1.05 · a2/√3` | 1.00×（理论值 0.0175） |
| 镜像等价 | 「上半 ∪ y镜像」与完整阵列的对称差面积 `< 1e-6 mm²` | 6.8e-15 mm² |

### 4.2 `method='dxf'` 的参数 —— ⚠️ 原计划的说法**已过时**

计划写「`cst_solver.import_dxf()` 只收 `filename, add, HealSelfIntersections, ...`，
**没有** `component` / `material` / `height` / `translate`」。

**实测（2026-09-15，`cst_solver/import_export/io.py:91-113`）：前三个都有**，
而且 `component` / `material` / `height` 还支持**列表**（逐图层给不同材质/厚度），
CST 侧走 `.AddLayer`。原脚本 `lens_build.py:270` 本身就是
`app.dxf_import(lens_dxf, add='True', component='gridlens', height='h')`。

所以这条「硬缺口」实际只剩 **`translate`** —— 而平移在原脚本里本来就是**单独一句**
`app.translate(...)`（`lens_build.py:327-328`），不是导入参数。
`build_grin_lens()` 已按原脚本的顺序把这两句平移放在导入之后，不需要新参数。

**剩下的**：`method='dxf'` 只差一个入口函数（复用 `build_grin_lens()` 的后半段）。

---

## 5. 出口判据

1. `builders/lens.py` 存在，是无状态纯函数，已在 `builders/__init__.py` 导出并加入 `__all__`；✅
2. 两种 `method` 都有可复现的验收结论（与对应旧 notebook 的几何对比）：
   - `'hexagon'` ✅ —— 与原脚本 `lens_build.py` **逐点等价**（孔心/孔半径/椭圆参数逐元素相等）
     + CST 调用序列**逐条等价** + 三道自查全过；
   - `'dxf'` ❌ 未做；
3. `GRINLensAntenna.build_all()` 端到端跑通，`get_messages()` 与 `Rebuild()` 后均为空；❌ **需要 CST**
4. 多边形绕向 CCW、阵列范围覆盖整个基板（ARCHITECTURE §6 硬约定 1、3）；✅ 透镜孔交给
   `mesh_grid.hex_grid.create_hex_polygon`（统一 CCW）；阵列范围与 §4.1 的列范围判据一致；
5. 同步 `docs/packages/topo_modeler.md` / `templates.md`；🔄 进行中

---

## 6. 前置阻塞 —— ✅ **已解除**

> 旧 `next_plan` §7 明确：本阶段前置为
> **阶段 3 几何验收通过** + **VPC 语义决策完成** + **面编号方案确定**。

这三条就是阶段 4 的 **T1**（几何对比 ✅）、**T8**（VPC 语义 ✅）、**T9**（面编号 ✅）
—— 全部已完成，所以几何层可以开工。**唯一余下的 CST 阻塞**是端到端验证（判据 3）。

---

## 7. 未做的事（留痕）

| 项 | 为什么没做 |
|---|---|
| `GRINLensAntenna` 模板 | 端到端必须真 CST 才能验；在算力受限期间做出来只会是「从未被真实调用过」的代码（阶段 4 的教训） |
| `method='dxf'` 入口 | 与 `'hexagon'` 共用后半段，等有人真的要从 DXF 造透镜时再补；否则同样是没验证过的分支 |
| CST 步骤的真机验证 | 用户要求暂不启动新的 CST 计算 |

### ⚠️ 收编时发现的坑（避免重踩）

1. **最外圈孔本来就轻微相叠。** 参考配置下 `2·r_out / a2 = 1.011 > 1`。
   我最初把它写成「孔半径过大」的**硬校验**，结果把参考配置自己挡在门外 ——
   已降级为 `GrinLensSpec.notes()` 里的**非阻断提示**。
   真正决定孔阵列可用性的是覆盖自查，不是这条。
2. **`d_out_mode='ec_a'` 会让外侧 63% 的孔全是 `r2`。** 原脚本注释提到过，
   但默认就是这样。不写清会让人以为孔半径沿全长渐变。
3. **DXF 只写上半平面，判据是「孔心 `y ≥ 0`」而不是「整孔在上半平面」。**
   压在 `y=0` 上的那行孔，下半几个顶点本来就在 `y<0` —— 这是对的，
   镜像自查用 ~1e-15 mm² 的对称差面积证明了「上半 ∪ y镜像 == 完整阵列」。
4. **`HexGridVisualizer.__init__` 会自建一张 14×12 的空画布。** 库函数里必须
   `plt.close(vis.fig)`，否则调用方会莫名多出一张空白图（原脚本的注释里也记着）。
