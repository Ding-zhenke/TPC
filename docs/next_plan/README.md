# TPC 实施计划总纲（唯一计划入口与阶段状态权威）

> **本文件是唯一计划入口。** 动代码之前先读这里，确认需求属于哪个阶段。
> 每个阶段的详细任务见 [`stages/`](./stages/) 下对应文件；本文只给阶段划分与状态。
> 最后更新：2026-09-15（本轮整理：合并 7 份历史计划、新增阶段 4「验收与缺陷清账」）

---

## 0. 三十秒结论

| 问题 | 答案 |
|---|---|
| **一共几个阶段？** | **9 个（阶段 0–8）**，见 §1 |
| **现在在哪？** | **阶段 4「验收与缺陷清账」进行中** —— 阶段 0–3 代码就绪但**验收未完成** |
| **下一步做什么？** | 见 §4 的 TODO 清单；最优先是**实体级几何对比** |
| **旧的 7 份计划还算数吗？** | 不算实施依据。它们是**详细规格与历史记录**，编号对照见 §3 |
| **为什么又改编号？** | 因为「验收」和「库加固」两件事原本散落在各文档、没有阶段归属，见 §3 对照表 |

> ⚠️ **编号警示**：本仓库历史上有过**四套互相冲突的阶段编号**，曾造成严重误读
> （详见 [`00_旧版README_整理与历史记录.md`](./00_旧版README_整理与历史记录.md) §3）。
> 从本轮起**只有本文件的编号为准**，其他文档里的「阶段 N」一律需要先查 §3 对照表。

---

## 1. 阶段总表

| 阶段 | 名称 | 负责什么（一句话） | 核心交付 | 状态 | 前置 |
|---|---|---|---|---|---|
| **0** | 准备与规范 | 立规矩 | `topo_modeler/`、`templates/` 包结构 + 代码风格规范 | ✅ 完成 | — |
| **1** | 坐标统一层 | 让晶格坐标只有一个来源 | `mesh_grid/tri_grid/topo_path.py`（TopoPath + 16 项单测） | ✅ 完成 | 0 |
| **2** | 基础建模引擎 | 把「路径 + 参数」变成完整模型 | `NameManager` + 4 个 builders + `TopoModeler` 骨架 | ✅ 完成（**未验收**） | 1 |
| **3** | 基础模板层 | 跑通两个完整器件 | 3 个部件 builders + `StraightWaveguide` + `UnitAntenna` | ✅ 完成（**未验收**） | 2 |
| **4** | **验收与缺陷清账** | 证明阶段 2/3 真的对，并清掉暴露的缺陷 | 几何 / S 参数 / 远场三组验收判据 + 关键决策 | 🔄 **进行中** | 3 |
| **5** | cst_solver 加固与借鉴落地 | 让库「写错就提示」而不是「读文档才知道」 | 运行时守卫层 + 结构化错误契约 + 外部参考资料落地 | ❌ 未开始 | 4（阶段 3 的验收） |
| **6** | 复杂模板层 | 做 GRIN 透镜天线 | `LensBuilder` + `GRINLensAntenna` 模板 | ❌ 未开始 | 4、5 |
| **7** | 工具层 + 报告 + 审计 | 让仿真「跑得动、看得见、留得下」 | 结果读取 / YAML / 扫描 / 批量 / GA + 报告引擎 + 审计落盘 | ❌ 未开始 | 6 |
| **8** | 复杂结构 + 旧代码迁移 | 从「两个器件」到「整个课题」 | 多路径 / 多端口 / 功分器 / MZI + 旧 notebook 迁移 | ❌ 未开始 | 7 |

> **「代码就绪」≠「验收通过」**：阶段 2/3 的判据是「与参考工程 `AB_feed.cst` / `BA_feed.cst`
> 对比，关键尺寸偏差 < 0.1%」，外加 S 参数谐振峰 < 1 GHz、远场主瓣 < 5°。
> 这三条**从未完整执行过** —— 这正是阶段 4 存在的理由。

---

## 2. 各阶段详细任务

| 阶段 | 详细文件 |
|---|---|
| 0 | [`stages/00_阶段0_准备与规范.md`](./stages/00_阶段0_准备与规范.md) |
| 1 | [`stages/01_阶段1_坐标统一层.md`](./stages/01_阶段1_坐标统一层.md) |
| 2 | [`stages/02_阶段2_基础建模引擎.md`](./stages/02_阶段2_基础建模引擎.md) |
| 3 | [`stages/03_阶段3_基础模板层.md`](./stages/03_阶段3_基础模板层.md) |
| 4 | [`stages/04_阶段4_验收与缺陷清账.md`](./stages/04_阶段4_验收与缺陷清账.md) |
| 5 | [`stages/05_阶段5_库加固与借鉴落地.md`](./stages/05_阶段5_库加固与借鉴落地.md) |
| 6 | [`stages/06_阶段6_复杂模板层.md`](./stages/06_阶段6_复杂模板层.md) |
| 7 | [`stages/07_阶段7_工具层与报告审计.md`](./stages/07_阶段7_工具层与报告审计.md) |
| 8 | [`stages/08_阶段8_复杂结构与旧代码迁移.md`](./stages/08_阶段8_复杂结构与旧代码迁移.md) |

---

## 3. 编号对照表（本仓库最大的历史坑，务必先查）

本方案把**「验收」与「库加固」提升为独立阶段**，因此阶段 4 之后整体后移：

| 本方案 | 旧 06（曾唯一权威） | 旧 05 | 旧 04 | `cst-runtime-cli` 采纳计划 | 备注 |
|---|---|---|---|---|---|
| 阶段 0 准备与规范 | 阶段 0 | 阶段 0 | （无） | — | 内容一致 |
| 阶段 1 坐标统一层 | 阶段 1 | 阶段 1 | 阶段一（部分） | — | 内容一致 |
| 阶段 2 基础建模引擎 | 阶段 2 | **阶段 3** | 阶段二 | — | 内容一致 |
| 阶段 3 基础模板层 | 阶段 3 | **阶段 4** | 阶段三（部分） | — | 内容一致 |
| **阶段 4 验收与缺陷清账** | （散落在旧 README §6） | — | — | **批次 0 / 批次 5** | **新增阶段** |
| **阶段 5 库加固与借鉴落地** | （散落在 06 §九 / §14.5） | — | 04 §3 库层任务 | **批次 1 / 2 / 3** | **新增阶段** |
| 阶段 6 复杂模板层（透镜） | **阶段 4** | **阶段 5** | 阶段三（部分） | — | ⚠️ 旧号「阶段 4」= 本方案阶段 6 |
| 阶段 7 工具层 + 报告 + 审计 | **阶段 5** | **阶段 6–7** | 阶段三（部分） | **批次 4** | ⚠️ 旧号「阶段 5」= 本方案阶段 7 |
| 阶段 8 复杂结构 + 迁移 | **阶段 6** | **阶段 8** | 阶段四 | — | ⚠️ 旧号「阶段 6」= 本方案阶段 8 |

**读旧文档时的速查**：

- 旧文档说「阶段 4」→ 本方案**阶段 6**（GRIN 透镜）
- 旧文档说「阶段 5」→ 本方案**阶段 7**（工具层）
- 旧文档说「阶段 6」→ 本方案**阶段 8**（复杂结构 + 迁移）
- 旧文档说「阶段 0/1/2/3」→ 本方案**同号**

---

## 4. 阶段 4 待执行清单

> **本节只列还没做的。** 已完成项见 §4.3，其完整实测记录与踩坑见
> [`stages/04_阶段4_验收与缺陷清账.md`](./stages/04_阶段4_验收与缺陷清账.md)。

### 4.1 待执行：不需要求解器

- [ ] **T5 `unit_antenna` 阵列范围** —— `unit_antenna.py:122` 用 `path.get_array_range()`
      推导（与已修的 `StraightWaveguide` 同源）。**参考基准已找到**（见下），
      但改动前需先理清 TPC 的 `straight_length`/`arm_length`/`bend_angle`
      与参考 `x1/y1/x2/y2` 的对应关系（`xup` 随拓扑变，25(AB)/26(BA)）；
      **且无法用 CST 验证时不宜动几何**，故暂缓。
- [ ] **T10 `templates` 包名风险** —— 顶层包名过于通用，改名 `topo_templates` 并保留一个版本周期的 shim。

**T5 参考基准（已查明，2026-09-15）**：`拓扑光子晶体模型\硅基\普通单元天线\` 下三个工程
（AB/BA 120° cylinder、AB 120° Feed_antenna）的参数表一致给出：

| 参考工程 | `xup` | `yup` | `ydn` | `ymax_up/dn` |
|---|---|---|---|---|
| `Ant1_D_AB_120_cylinder-DF` | 25 | **14** | **14** | ±2.94 |
| `Ant1_D_BA_120_cylinder-DF` | **26** | **14** | **14** | ±2.94 |
| `Ant1_D_AB_120_Feed_antenna-DF` | 25 | **14** | **14** | ±2.94 |

即 `yup = ydn = y1 = width = 14`（覆盖整个基板宽，与直波导参考一致）；
`xup` 为 25（AB）/ 26（BA）—— **随拓扑不同而非固定 `length+int(width/2)`**，改时需按拓扑取值。

### 4.2 待执行：需要求解器（🔴 计算资源受限）

> **用户约束**：计算资源紧张，S 参数对比只挑个别几个点，不做全扫；**本轮仿真跑完即止，不再启动新的**。

- [ ] **T7 远场主瓣偏差 < 5°** —— 依赖 T4（远场监视器代码已修，CST 验证待做）与 T6。

### 4.3 已完成（本轮移出待办）

| 项 | 结论 | 证据 |
|---|---|---|
| **T1** 实体级几何对比 | 26 个共有实体中 13 个**逐字节一致**，关键尺寸偏差 **0%**（判据 < 0.1% 满足）；6 处表面差异经核对均为等价 | `scripts/compare_model_history.py`、`stages/04` §2.5 |
| **T2** 路径起点差一格 | ✅ 已修：`start(0,0)+move(length,'c')`，路径由 19 格→**18 格**，`p1x/p2x` 与参考 `px1/px2` 逐一相同 | `stages/04` T2 |
| **T3** `l1`/`l2` 按实体核对 | ✅ 结论：补偿**正确**，四个三角形孔尺寸与参考逐一相同；仅命名反转 | `stages/04` T3 |
| **T8** VPC 区域语义 | ✅ 已决**以参考工程为准**并实施（三处改动） | `stages/04` T8/T11 |
| **T11** 缺失的布尔运算 | ✅ 晶体∩VPC 求交已补，布尔序列 13→**15** 条（参考 17 条，差 2 条属构造路径不同、结果等价） | `stages/04` T8/T11 |
| **T6-prep** 显式监视器频点 | ✅ `configure_solver()` 已支持 `monitor_frequencies` | `topo_modeler/builders/solver.py` |
| **T4** 远场监视器 | ✅ **已修**（代码；CST 验证待后续）—— 远场监视器应走 `create_field_monitor('Farfield', ...)`，删除了手写的 `With Farfield … .Type "Broadband"` | 见 §4.4 第 11 条 |
| **T9** 面编号 | ✅ **已重新定性并完成** —— 硬编码 `'10'`/`'22'` **与参考工程完全一致**（参考用的就是这两个编号，端口定义块逐字相同），故**不替换**；改为在建端口前用 `get_picked_count('face')` 校验，编号失效时抛 `RuntimeError` 而非静默建错 | 见 §4.4 第 12 条 |
| **T6** S 参数对比 | ✅ **已完成** —— 时域求解（关自适应 + 稳态 −20 dB），两模型各跑一次，比较 4 个频点：**8 个点里 7 个在 1 dB 内**。S21（传输）4 个点全部 ≤ 0.73 dB；S11 3 个点 ≤ 0.49 dB，仅 350 GHz 点差 1.83 dB | 见 §4.5 |

> **几何至此与参考等价** —— 剩余可比差异只有实体**命名与 VPC 构造路径**（已核对多边形等价）。

### 4.4 本轮遇到的问题与更正（留痕，避免重踩）

| # | 问题 | 处理 |
|---|---|---|
| 1 | **`Insert` 语义被第三方资料记错** —— 实为**差集** `s1 − s2`（不删除 s2），某第三方整理的 VBA 参考写成 `s1 ∪ s2`，直接导致误判 VPC-B 形状 | 以 CST 官方帮助为准，写入 `docs/ARCHITECTURE.md` §6 第 2 条 |
| 2 | **VPC-A/B 上下半区与参考相反** —— 参考 VPC-A 是下半区（`vpc_a_area1` 多边形 + `vpca intersect vpc_a_area1` 两条独立证据），本库原为上半区 → 器件在 y 方向镜像 | 已改 `builders/vpc_region.py` |
| 3 | 🔴 **基板/VPC 半宽只有 1 行晶格**（参考 14 行）—— 根因是 `width` 只被用来算阵列范围，没用来定基板/VPC 宽度；阵列远在基板之外 | 已改为 `y_margin = f'{width}*e2'` |
| 4 | **多出一个 `substrate` 实体** —— 参考没有 substrate，其硅即 `vpca`（图形化）；多一块未图形化的整幅硅会把光子晶体孔洞**全部填平** | 本模板改为「两个 VPC 区域即硅本体」，`build_substrate()` 仍留在库中 |
| 5 | **`MaximumNumberOfGPUs 0` 非法** —— CST 报 `Invalid number of hardware devices`；关闭 GPU 只能写 `HardwareAcceleration "False"` | 已修 `set_gpu_acceleration(enable=False)`，并约定 `gpus=0` 表示关闭 |
| 6 | **`cst.interface` 导入顺序** —— 必须在 `cst_solver` **之后**导入（后者会在导入时把 CST 的 `python_cst_libraries` 加入 `sys.path`），否则 `ModuleNotFoundError: No module named 'cst'` | 已修新脚本；写新脚本时注意 |
| 7 | **S 参数与监视器频点无关** —— S 参数来自端口信号、覆盖整个频段，监视器只决定**场快照**取样点。我先前"必须统一监视器频点才能比 S 参数"的说法**有误** | 已在 `configure_solver` docstring 与提交信息中更正 |
| 8 | **参考工程没有可复用的结果** —— `AB_feed` 的 `Result/` 仅 0.3 MB/8 文件；全盘只有两个无关 `.s2p`；两个 `AB_feed.ipynb` 也没有 S 参数输出 → **两边都得算** | 记入 §4.2 与 `stages/04` |
| 9 | **时域求解器无法"只算几个点"** —— 它一次算完整个频段；"只挑几个点"只能指**只比较几个点**。要真"只算几个点"须换频域求解器（FDSolver） | 记入 `stages/04` §5 成本方案 |
| 10 | **文档生成器在 GBK 控制台直接崩** —— `print("🔍 …")` 抛 `UnicodeEncodeError` | 已去 emoji（实测 GBK 下 exit=0） |
| 11 | **远场监视器一直写错了对象** —— `topo_modeler` 手写 `With Farfield … .Type "Broadband"`。但 `Farfield` 是**后处理对象**（FarfieldPlot / FarfieldCalculator），不是建监视器的对象；官方 `Monitor` 的 **FieldType 枚举里本就含 `"Farfield"`**。而 `cst_solver` 的 `define_monitor` 早已正确实现该路径（`_FIELD_CONFIG['Farfield']`） | 删除该函数，统一走 `create_field_monitor()`；**代码已改，CST 验证待后续** |
| 12 | **面编号"缺陷"其实不是缺陷** —— 我与参考工程逐条核对后发现：参考用的正是 `'10'`/`'22'`，端口定义块与本库**逐字相同**（因为 `wg1` 的 brick 两边几何一致）。原计划"替换成坐标方案"会**偏离参考** | 不替换；改为加校验（`get_picked_count` 兜住编号失效） |

### 4.5 T6 实测结果（S 参数对比，已完成）

**设置**：两个模型走**同一个**配置函数 —— 时域求解器、关网格自适应、稳态限制 −20 dB、
频段 310–370 GHz；参考工程用**副本**（原件未动）。各跑一次，比较 4 个频点。

| 频点 | S1,1 本库 | S1,1 参考 | Δ | S2,1 本库 | S2,1 参考 | Δ |
|---|---|---|---|---|---|---|
| 310 GHz | −14.973 | −14.781 | 0.192 | −2.063 | −1.936 | 0.127 |
| 330 GHz | −20.817 | −20.326 | 0.491 | −3.939 | −3.885 | 0.054 |
| **350 GHz** | −11.838 | −10.008 | **1.830** ⚠️ | −17.779 | −18.511 | 0.731 |
| 370 GHz | −11.538 | −11.842 | 0.304 | −7.371 | −7.913 | 0.542 |

（单位 dB。8 个点里 **7 个在 1 dB 内**。）

**结论**：

- **传输 S21 四个点全部 ≤ 0.73 dB** —— 与参考高度一致，说明**几何对齐成功**；
- 反射 S11 三个点 ≤ 0.49 dB，**仅 350 GHz 一处差 1.83 dB**。

**⚠️ 两点必须说清的局限**：

1. **1 dB 是我设的代理容差，不是原判据。** 计划原本的判据是「谐振峰偏差 < 1 GHz」——
   那是**频域上的峰值位置对比**，需要先做峰值识别；用「某几个频点的 dB 差」代理它，
   只能发现结构性错误，不能直接判定是否满足原判据。
2. **S11 在反射低谷附近对微小差异极其敏感** —— 350 GHz 处 S11 已到 −11 dB 量级，
   1.8 dB 的差别可能来自数值误差/网格差异而非结构错误。要判定这一点，
   需要看**曲线形状**（是否只是整体平移）而不只是单点数值。

**成本记录**（供后续估算）：单次时域求解约 **5300 秒 CPU 时间**、峰值内存约 **2.4 GB**，
用的是"关自适应 + 稳态 −20 dB"的省时配置。完整自适应版本会显著更久。

### 4.6 阶段 4 出口判据（什么时候算做完）

1. 三组验收判据全部有**可复现的实测结论**（几何 ✅ / S 参数 ✅（见 §4.5）/ 远场 ❌ 待 T7）；
2. T2、T8 两个决策已落地 ✅；
3. 其余 TODO 要么完成，要么明确标注「不做」并给出替代方案。

> **余下阻塞**：T7（远场）依赖 T4 的 CST 验证，而用户已要求**暂不启动新的 CST 计算**，
> 故阶段 4 的"远场"一组暂挂起；T5、T10 属可延后项。**阶段 4 实质只剩 T7 未做。**

---

## 5. 本目录文件角色

| 文件 | 角色 |
|---|---|
| **`README.md`（本文件）** | ⭐ **唯一计划入口与阶段状态权威** |
| `stages/` | ⭐ 各阶段详细任务（9 份） |
| `00_旧版README_整理与历史记录.md` | 历史记录：旧计划整理过程、25 处计划硬伤修正、16 处代码缺陷修正。**只作历史与证据** |
| `01_Python建模拓扑光子晶体指南.md` | 重构前 notebook 工作流教程（历史资料）。可作「旧 notebook 参数事实」参考 |
| `02_函数优化清单.md` | 53 条函数级议题（已实施 19 条）。剩余价值：命名/魔法数字、YAML、批量建模、结果绘图、参数扫描 |
| `03_AI技能知识库.md` | 已被 `skills/user/tpc-usage.md` 取代。待迁移：notebook 文件名约定、物理概念与频段、症状→病因→动作调试表 |
| `04_详细实施计划清单.md` | 112 条详细任务。**其 §3 的 28 条库层任务已被阶段 5 吸收**；阶段编号不再使用 |
| `05_阶段实施计划.md` | 已被 06 取代；与 06 有 10 组阶段冲突。仅作历史 |
| `06_整合实施计划_分阶段分模块.md` | **模块级详细规格**（各模块的接口、输入输出、验收标准）。编号已后移，见 §3 对照 |
| `cst_solver_TODO_LIST.md` | `cst_solver` 未覆盖的 CST VBA 对象清单（阶段 5 的输入） |

本仓库外的相关资料：

| 位置 | 内容 |
|---|---|
| `../cst-runtime-cli-adoption-plan.md` | 借鉴 `cst-runtime-cli` 的实施计划（批次 0–5），已并入阶段 4 / 5 |
| `../cst-runtime-cli-install-checklist.md` | 该包的安装调研（结论：不安装，只借鉴设计） |
| `D:\成电博士生涯\自动建模算法尝试\cst-runtime-cli\`（仓库外） | 已 clone 的参考实现；其 `devkit/references/` 含官方 VBA / Python API 整理 |

---

## 6. 相关文档

- 总架构与包职责 → [`../ARCHITECTURE.md`](../ARCHITECTURE.md)
- 每个包一份说明 → [`../packages/`](../packages/)
- **开发工作流（改代码前必读）** → [`../../skills/developer/WORKFLOW.md`](../../skills/developer/WORKFLOW.md)
- `cst_solver` 维护技能 → [`../../skills/developer/cst-solver-dev.md`](../../skills/developer/cst-solver-dev.md)
- 使用手册 → [`../../skills/user/tpc-usage.md`](../../skills/user/tpc-usage.md)
